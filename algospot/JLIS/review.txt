내 솔루션대로 하면 [c1, c2, c3, c4, b] + a 를하려면 추가하려면 c4 < min(a, b)
교재의 솔루션 대로라면 a + [b, c1, c2, c3, c4]를 하려면 max(a, b) < c1

이 때 기저 case를 처리하기 위해 -inf를 사용하면 c4 < min(a, b) 가 애매해지고,
기저를 index가 가장 큰 값으로 생각해서 inf를 넣는다 해도 내가 정의한 dp의 점화식을 뒤에서부터는
풀 수가 없다.

교재의 솔루션 대로 dp를 정의하는 게 더 편할꺼 같다.
